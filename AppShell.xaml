<Shell
    x:Class="CRESANA.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:CRESANA.Views">

    <ShellContent Route="login"       ContentTemplate="{DataTemplate views:LoginPage}" />
    <ShellContent Route="patientForm" ContentTemplate="{DataTemplate views:PatientFormPage}" />
    <ShellContent Route="main"        ContentTemplate="{DataTemplate views:MainPage}" />
</Shell>
